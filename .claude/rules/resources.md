---
paths:
  - "resources/**/*.md"
---

# Resources Rules

- Templates live in `resources/templates/` — use `{{date}}`, `{{title}}` placeholders
- Scripts live in `resources/scripts/`
- Templates are acceptable orphans (not linked from analysis docs)
- One canonical template per type — no duplicates
